function table=results_table_func(results)
    feval_dist=[...
        mean(results(:,1)),...
        min(results(:,1)),...
        median(results(:,1)),...
        max(results(:,1))];

    table=[feval_dist,mean(results,1)];
return
